# Copy this file to .env and fill in real values, or export these
# directly in your shell profile (~/.zshrc, ~/.bashrc, etc).

NODEFALL_JWT_SECRET=change-me-to-a-real-secret
NODEFALL_DB_URL=postgres://nodefall:devpassword@localhost:5432/nodefall?sslmode=disable
